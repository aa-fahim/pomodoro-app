import styled from "styled-components/native";
import { themes } from "./themes";

const HorizontalBreak = styled.View`
  border-bottom-color: black;
  border-bottom-width: 1;
`;

export default HorizontalBreak;
